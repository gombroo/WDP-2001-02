/* selectors */
export const getAll = ({ galeries }) => galeries;

/* reducer */
export default function reducer(statePart = [], action = {}) {
  switch (action.type) {
    default:
      return statePart;
  }
}
